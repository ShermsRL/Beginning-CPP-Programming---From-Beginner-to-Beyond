#include <iostream>

using namespace std;

int age {18}; // Global variable is declared outside function

int main() {
    
    int age {16}; // Having a same local name variable will overwrite the global variable
    cout << age << endl;
    return 0;
    
}
